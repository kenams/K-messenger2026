plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'com.kahdigital.kssenger'
version = '1.0.0'

android {
  namespace "com.kahdigital.kssenger.signal"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
    testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
  }

  // libsignal-android uses newer Java library APIs. Keep desugaring explicit on
  // the native module itself so standalone instrumentation does not depend on
  // unrelated Expo modules inheriting app-level Gradle configuration.
  compileOptions {
    coreLibraryDesugaringEnabled true
  }
}

repositories {
  maven { url 'https://build-artifacts.signal.org/libraries/maven/' }
  mavenCentral()
}

dependencies {
  coreLibraryDesugaring "com.android.tools:desugar_jdk_libs:2.1.5"
  implementation "org.signal:libsignal-client:0.100.0"
  implementation "org.signal:libsignal-android:0.100.0"

  androidTestImplementation "androidx.test:core:1.6.1"
  androidTestImplementation "androidx.test.ext:junit:1.2.1"
  androidTestImplementation "androidx.test:runner:1.6.2"
}
