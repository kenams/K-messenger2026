# K-ssenger — passation Codex — 2026-09-16 soir

**Lis ce fichier en premier.** Session Claude interrompue par limite d'usage hebdomadaire. Repo propre, tout committé/poussé, web déployé et vert.

## État exact
- Repo : `C:\Users\kenam\Application-Projet-K\K-messenger2026` (monorepo npm workspaces : `apps/mobile` Expo SDK 54, `apps/server`, `apps/e2e`, `packages/contracts`).
- Branche : `feature/device-linking-scaffold`, dernier commit **`f4ee6b7`**, poussé sur `origin`, working tree clean.
- Web prod : **https://k-ssenger.expo.app** — déployé sur `f4ee6b7`, `npm run ship:web` (racine) = **22/22 E2E verts**.
- Backend : Neon `late-flower-65059830` + Render `kssenger-server.onrender.com` (peut être en veille — réveiller via `/health` avant tout test).
- APK Android : release GitHub `v2.0.0-beta.12` marquée `latest`, mais **construit sur un commit ANTÉRIEUR** aux 3 derniers commits de ce soir (theme fixes + pulse notif). Lien stable (ne change jamais) : `https://github.com/kenams/K-messenger2026/releases/latest/download/K-ssenger-latest.apk`. Le QR code de l'app (`apps/mobile/src/features/profile/MobileAppQr.tsx`) pointe dessus, rien à modifier là.
  - **Quota EAS Build gratuit épuisé jusqu'au 01/10/2026.** Un nouveau build cloud (`npx eas build --platform android --profile preview --non-interactive` depuis `apps/mobile`) est bloqué jusque-là, sauf à payer (dépense réelle → à valider avec Kenams, ne pas décider seul).
  - Alternative testée ce soir : build Gradle **local** depuis `C:\kssenger` (chemin court obligatoire, sinon erreurs CMake "filename too long"), avec le JDK embarqué d'Android Studio (JBR 21) et un truststore Avast patché déjà présent quelque part sur cette machine (Avast casse les builds Gradle sinon — cf. mémoire projet `feedback_avast_gradle_ssl.md`). Si tu dois rebuild l'APK avant le 01/10, pars de là.

## Ce qui a été fait aujourd'hui (dans l'ordre)
1. **Mode jour/nuit réactif complet** (`d99035d`) : `ThemeProvider` + `useTheme()` (`apps/mobile/src/theme/ThemeProvider.tsx`), palette light/dark dans `theme/tokens.ts`, tous les écrans convertis `StyleSheet.create` statique → `useMemo` par composant. Toggle Clair/Sombre/Système dans l'onglet **Moi** (`App.tsx`).
2. **Palette calée sur la vraie identité kah-digital.ch** (extraite en direct du site avec `getComputedStyle`) : dark bg `#0A0908`, texte `#F5F1E8`, accent or `#D6B36A`, teal `#7FB8C7` (`palette.music`), terracotta `#D28A68` (`palette.wizz`). Si jamais quelqu'un retouche la palette : ce sont les valeurs de référence, pas des choix arbitraires.
3. **Bug contacts vides au premier login** : cause trouvée = **4 profils "Kenams" fantômes en base** (restes de scripts de seed), dont un (`kenams_app`, id `85db1ffe`) ajouté comme contact "Kenams" chez les bots en plus du vrai compte (`kenamz`, id `43247878`) → `.first()` tombait parfois sur le fantôme. **Nettoyé en base Neon** (22 lignes de contacts orphelins supprimées). Il reste 2 profils fantômes inoffensifs (`kenams`/`ec54fee7`, `kah`/`9a79b6a4`) liés à des faux bots `kbot_alpha`/`kbot_beta` — pas de bots réels, pas d'impact, nettoyage optionnel.
4. **Musique Last.fm cassée en prod** : cause = `scripts/ship-web.mjs` exportait sans `--clear`, donc le cache Metro global (`%TEMP%\metro-cache`) servait un vieux bundle qui n'avait jamais la clé Last.fm malgré plusieurs déploiements "réussis". **Fix : `--clear` ajouté à l'export.** Vérifié en direct : `curl "https://ws.audioscrobbler.com/2.0/?method=user.getrecenttracks&user=kah42&api_key=...&format=json"` répond bien, et le bundle déployé contient la clé (`6cf2de22fd7a199162deaeab4b3bcb2d`).
5. **Bug de navigation trouvé par Kenams ce soir** (`17c3d82` + `c795a33`) : `useMyProfile().refresh()` (`apps/mobile/src/features/profile/useMyProfile.ts`) mettait `loading=true` sur CHAQUE rafraîchissement de fond (déclenché toutes les ~15s par le poll now-playing dans `useNowPlayingSync`), ce qui faisait basculer `AuthenticatedRoot` (`apps/mobile/src/Root.tsx`) sur un écran `<Loading>` plein écran → démontait `<App/>` → perdait tout son état local (`tab`, `editingProfile`, etc.) → l'utilisateur se retrouvait renvoyé sur l'onglet Contacts en plein milieu d'une action. **Fix : le loader plein écran ne s'affiche plus que tant qu'aucun profil n'a encore été chargé une première fois** (`profile.loading && !profile.profile`, idem pour `profile.error`). Effet de bord découvert en cascade : le retour automatique à l'écran principal après avoir sauvegardé son profil dépendait accidentellement de ce remount — corrigé en appelant `onBack()` explicitement après la sauvegarde dans `ProfileEditScreen.tsx`.
6. **Clarté Spotify/Last.fm** (`17c3d82`) : note "Nécessite un compte Spotify Premium" ajoutée sous le bouton Spotify (l'API Spotify refuse la lecture en direct aux comptes gratuits, confirmé sur le dashboard dev). Lien "Pas de compte Last.fm ? Le créer en 30 secondes →" ajouté (ouvre `https://www.last.fm/join`), pour réduire la friction d'inscription — demande explicite de Kenams : "il faut que les gens se prennent le moins possible la tête".
7. **Notification K-Pulse qui pulse jusqu'à être vue** (`f4ee6b7`) : nouveau hook `usePulseUntilSeen()` dans `apps/mobile/src/theme/components.tsx` — fait pulser doucement (scale loop, respecte `useReducedMotion`) le bandeau "⚡ K-Pulse reçu de …" tant qu'il est affiché, et sur web fait clignoter le titre de l'onglet du navigateur tant que la page n'est pas au premier plan (`document.visibilityState !== 'visible'`). Branché dans `apps/mobile/src/features/contacts/MsnContactsScreen.tsx`. **Pas encore vérifié par Kenams en vrai** (déployé mais pas testé main dans la main ce soir, contrairement au reste) — première chose à confirmer à la reprise.

## Reste ouvert / connu, non corrigé
- **Lenteur de changement d'onglet** : chaque onglet (`App.tsx`, rendu conditionnel `{tab === 'x' && <Screen/>}`) démonte/remonte complètement au changement, donc sockets + tout l'état se recréent à chaque bascule. Piste identifiée, **pas corrigée** — garder tous les onglets montés en permanence casserait potentiellement K-Feed (vidéo) et K-Map (GPS) en arrière-plan, risque de régression trop élevé pour un correctif à l'aveugle sans test sur device réel.
- **`profiles.lastfm_username` de Kenams** : confirmé fonctionnel côté prod ce soir (le titre "Diam's — Marine (version 2006)" remontait en direct sous son nom pendant la session).
- **APK Android en retard de 3 commits** sur le web (theme final + navigation fix + pulse notif) — cf section État exact ci-dessus pour le blocage EAS.
- **Flakiness E2E temps réel préexistante** (`chats.spec.ts`, `kpulse.spec.ts`, `social.spec.ts` selon les runs) : pattern déjà documenté ailleurs dans le repo (un événement socket qui n'arrive pas toujours dans le délai du test, jamais deux fois le même test). Pas un bug produit confirmé — vérifié aux captures que les actions partent bien réellement. Si ça flake encore, relancer une fois avant de creuser.

## Commandes utiles
- Déploiement web complet (obligatoire avant tout push jugé "livré") : `npm run ship:web` depuis la racine. Fait : typecheck → 92 tests serveur → gate statique → export web avec `--clear` → déploiement EAS Hosting → réveil Render → suite E2E Playwright complète contre la prod. Zéro deploy sans que ça finisse vert (règle absolue du projet).
- Typecheck seul (rapide) : `npm run typecheck --workspace apps/mobile`.
- Build APK cloud (bloqué jusqu'au 01/10 sauf accord dépense) : `cd apps/mobile && npx eas build --platform android --profile preview --non-interactive`.
- Comptes de test prod : Kenams `kenams42+kssenger@gmail.com` / `Kss--f4BAds_-26` ; 10 bots mdp commun `KssBot2026!` (Léa `+kss-lea`, Karim `+kss-karim`, etc., toutes en alias `+…` de kenams42@gmail.com).

## Règles du projet à respecter sans exception
- **Zéro deploy sans Fulltest/`ship:web` vert.**
- Un Fulltest inclut TOUJOURS le cycle complet : session déconnectée → login → 100% des écrans/fonctions → **logout** → **re-login**. Ne jamais dire "testé" sans avoir fait ce cycle en entier.
- Aucune couleur codée en dur hors `apps/mobile/src/theme/tokens.ts` — tout doit passer par `useTheme()`.
- Ne jamais recréer le compte Stripe (`acct_1TDQW0GSZgm5QCNL`) ni toucher un autre projet Neon que `late-flower-65059830` pour ce produit.
